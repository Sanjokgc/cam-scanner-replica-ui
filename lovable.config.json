{
  "name": "cam-scanner-replica",
  "version": "1.0.0",
  "description": "PDF to Word conversion service",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "install": "npm install && pip install -r backend/requirements.txt"
  },
  "engines": {
    "node": ">=16.0.0",
    "python": ">=3.11.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1"
  }
} 